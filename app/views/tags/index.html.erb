<h1>All Tags</h1>
<ul id="articles">
  <% @tags.each do |the_tag| %>
    <li>
      <%= link_to the_tag.name, tag_path(the_tag) %>
      <%= link_to "delete?", tag_path(the_tag), 
					  method: :delete, 
					  data: {confirm: "Really delete the tag?"} %>
    </li>
  <% end %>
</ul>

